import * as fs from 'fs';
const input = fs.readFileSync("/dev/stdin", "utf8");
const a = +input.split('\n')[0];
const b = +input.split('\n')[1].split(' ')[0];
const c = +input.split('\n')[1].split(' ')[1];
const s = input.split('\n')[2];
console.log("%d %s", a+b+c, s);